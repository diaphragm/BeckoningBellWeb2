{
  "name": "beckoning-bell-web",
  "version": "1.0.0",
  "description": "My sweet Nuxt.js project",
  "author": "diaphragm",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=staging nuxt",
    "build": "NODE_ENV=staging nuxt build",
    "start": "NODE_ENV=staging nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "deploy": "NODE_ENV=staging nuxt build; firebase use staging; firebase deploy",
    "deploy:production": "NODE_ENV=production nuxt build; firebase use production; firebase deploy",
    "emulate": "firebase use staging; firebase emulators:start"
  },
  "dependencies": {
    "@nuxtjs/firebase": "^5.2.0",
    "@nuxtjs/google-adsense": "^1.1.3",
    "firebase": "^7.14.5",
    "nuxt": "^2.12.2",
    "sanitize-html": "^2.3.2",
    "vuefire": "^2.2.2",
    "vuetify-confirm": "^2.0.3",
    "vuetify-toast-snackbar": "^0.6.1"
  },
  "devDependencies": {
    "@nuxt/typescript-build": "^0.6.0",
    "@nuxtjs/eslint-config-typescript": "^1.0.0",
    "@nuxtjs/eslint-module": "^1.0.0",
    "@nuxtjs/vuetify": "^1.0.0",
    "babel-eslint": "^10.0.1",
    "elh-loader": "^0.0.1",
    "eslint": "^6.1.0",
    "eslint-plugin-nuxt": ">=0.4.2"
  }
}
